# FS8 2024 Cross Probe Stop Note

Date: 2026-06-19

## Decision

The local FreeSurfer 8.2.0 `2024_cross_probe` run was stopped manually. It had
spent more than 48 hours inside left-hemisphere `mris_fix_topology` with one CPU
core saturated and no new output progress in the log.

The completed FS8 outputs for 2018, 2022, the 2018/2022 base, and the 2018/2022
longitudinal subjects were not removed.

## Evidence

Last active stage:

```text
mris_fix_topology ... 2024_cross_probe lh
```

The log reports:

```text
95 defects to be corrected
CORRECTING DEFECT 16 (vertices=19307, convex hull=4912, v0=9575)
```

The same log also warns at earlier large defects:

```text
This often happens because cerebellum or dura has not been removed from wm.mgz.
This may cause recon-all to run very slowly or crash.
```

## Interpretation

This is a topology/skull-strip/white-matter failure mode for the 2024
`901_3di_mc_hr` input under standard FS8 `recon-all -all`. It should not be
interpreted as a near-complete high-quality segmentation.

Restarting the exact same command on the exact same input is not a good use of
compute. The likely next branches are:

- alternative 2024 T1 FFE candidates (`401_t1w_ffe`, `601_t1w_ffe`);
- ReconAny / recon-all-clinical;
- TIGERBx / OpenMAP-T1;
- harmonized preprocessing before surface reconstruction.

## Output State

Usable FS8 subjects:

- `2018`
- `2022`
- `kate_fs82_base`
- `2018.long.kate_fs82_base`
- `2022.long.kate_fs82_base`

Incomplete failure-case subject retained for forensic QC:

- `2024_cross_probe`
